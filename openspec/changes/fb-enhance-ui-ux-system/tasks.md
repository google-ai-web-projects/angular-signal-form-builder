## 1. Design System Foundation

- [x] 1.1 Extract design tokens from `DESIGN.md` into CSS custom properties (`src/styles.css`).
- [x] 1.2 Implement base typography styles (Inter font stack, sizing, leading, tracking).
- [x] 1.3 Add CSS utility classes for the new border radii (`rounded-none`, `rounded-sm`, `rounded-md`, `rounded-lg`, `rounded-pill`).

## 2. Core Components

- [x] 2.1 Refactor generic buttons to `button-primary` capsule shape with Action Blue color and click interaction.
- [x] 2.2 Create/Refactor secondary `button-dark-utility` and `button-secondary-pill`.
- [x] 2.3 Create `global-nav` component with pure black background.
- [x] 2.4 Create `sub-nav-frosted` component using parchment background and backdrop-filter blur.

## 3. Layouts & Rhythms

- [x] 3.1 Convert main page layouts to full-width, edge-to-edge product tiles.
- [x] 3.2 Implement alternating section backgrounds (light, parchment, dark).
- [x] 3.3 Ensure all generic layout shadows are removed, standardizing on the single product drop-shadow.

## 4. Review & Polish

- [x] 4.1 Perform visual review against structural breakpoints (1440px, 1068px, 834px, 640px, 419px).
- [x] 4.2 Validate token usage to ensure no hard-coded unauthorized colors or spacing exist.
